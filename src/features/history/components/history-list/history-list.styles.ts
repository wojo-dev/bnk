// history-list.styles.ts

import { spacing } from '@/features/shared/styles/tokens/spacing';
import { StyleSheet } from 'react-native';

export const historyListStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: spacing.md,
  },
});
